## Setup
1. Install `xolotl` from matlab add-ons
2. (Optional) Install parallel computing toolbox (if want to generate data on your own)
3. Run `initialize.m`
4. Run the demo files to test if the setup is working